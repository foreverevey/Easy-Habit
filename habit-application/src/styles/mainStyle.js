import React, {useState, useEffect, useContext} from 'react';
import {StyleSheet} from 'react-native';

export const mainStyle = (props) => StyleSheet.create({
  Checkbox:{
    fontSize:30,
    color: 'blue',
  },
});
